<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ReusableControlsTest.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ReusableControlsTest.Views"
    xmlns:views="clr-namespace:ReusableControlsTest.Views"
    Title="ReusableControlsTest"
    Shell.FlyoutBehavior="Disabled">
    <TabBar>
        <Tab
            Title="Home"
            Icon="home.png">
            <ShellContent
                ContentTemplate="{DataTemplate views:HomeTab}" />
        </Tab>
        <Tab
            Title="Devices"
            Icon="devices.png">
            <ShellContent
                ContentTemplate="{DataTemplate views:HomeTab}" />
        </Tab>
        <Tab
            Title="Life"
            Icon="life.png">
            <ShellContent
                ContentTemplate="{DataTemplate views:LifeTab}" />
        </Tab>
        <Tab
            Title="Routines"
            Icon="routines.png">
            <ShellContent
                ContentTemplate="{DataTemplate views:HomeTab}" />
        </Tab>
        <Tab
            Title="Menu"
            Icon="menu.png">
            <ShellContent
                ContentTemplate="{DataTemplate views:HomeTab}" />
        </Tab>
    </TabBar>

</Shell>
